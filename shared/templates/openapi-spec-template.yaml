openapi: 3.1.0
info:
  title: <feature> API
  version: 0.1.0
  description: |
    Generated from `.webstack/features/<feature>/plan.md` by /webstack:feature P3.
    Single source of truth — frontend codegen and backend implementation derive from this.

    NOTE: webstack does not bundle authentication. If this feature requires auth,
    add a `securitySchemes.bearerAuth` (or similar) entry under `components` and a
    `security: [{ bearerAuth: [] }]` block at the operation or document level, and
    declare `'401'` responses where applicable. See `docs/recipes/spring-security-auth.md`
    for the recommended Spring Security implementation.
servers:
  - url: http://localhost:8080
    description: Local dev
  - url: https://api-staging.example.com
    description: Staging
  - url: https://api.example.com
    description: Production

tags:
  - name: <resource>
    description: <one line>

paths:
  /api/v1/<resource>:
    get:
      tags: [<resource>]
      summary: List <resource>
      operationId: list<Resource>
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 20, minimum: 1, maximum: 100 }
        - name: cursor
          in: query
          schema: { type: [string, "null"] }
      responses:
        '200':
          description: success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/<Resource>List'
        '500': { $ref: '#/components/responses/InternalError' }
    post:
      tags: [<resource>]
      summary: Create <resource>
      operationId: create<Resource>
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/<Resource>CreateRequest'
      responses:
        '201':
          description: created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/<Resource>'
        '400': { $ref: '#/components/responses/BadRequest' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }
        '500': { $ref: '#/components/responses/InternalError' }

  /api/v1/<resource>/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [<resource>]
      summary: Get one <resource>
      operationId: get<Resource>
      responses:
        '200':
          description: success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/<Resource>'
        '404': { $ref: '#/components/responses/NotFound' }

components:
  schemas:
    <Resource>:
      type: object
      required: [id, createdAt]
      properties:
        id: { type: string, format: uuid }
        createdAt: { type: string, format: date-time }
        # add resource-specific fields here

    <Resource>CreateRequest:
      type: object
      required: []
      properties:
        # add request-specific fields here

    <Resource>List:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/<Resource>'
        nextCursor:
          type: [string, "null"]

    ProblemDetail:
      # RFC 9457 problem details — the single error model. Spring's GlobalExceptionHandler
      # emits these as `application/problem+json`, so springdoc's runtime spec matches this
      # shape and contract-drift-detective stays clean. See docs/backend/error-handling.md
      # and docs/cross-cutting/rest-api-design.md.
      type: object
      required: [status, title]
      properties:
        type: { type: string, format: uri, default: "about:blank" }
        title: { type: string }
        status: { type: integer }
        detail: { type: string }
        instance: { type: string, format: uri }
        violations:
          # Extension member (RFC 9457 §3.2) populated on 422 for field-level validation errors.
          type: array
          items:
            type: object
            required: [field, message]
            properties:
              field: { type: string }
              message: { type: string }

  responses:
    BadRequest:
      description: Bad request — request shape invalid
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetail' }
    NotFound:
      description: Resource not found
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetail' }
    UnprocessableEntity:
      description: Validation failed
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetail' }
    InternalError:
      description: Internal server error
      content:
        application/problem+json:
          schema: { $ref: '#/components/schemas/ProblemDetail' }
